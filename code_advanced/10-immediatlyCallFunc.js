/**
 * - Immediately invokes the function with 2nd set of ()
 *  - Évitez de polluer l'espace de noms ou la portée global lorsque cela est possible.
 */

(function immediatelyCall() {
    console.log("Immediately call the function ")
})()
